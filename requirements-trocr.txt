# Extra dependencies for the optional neural backend (--backend trocr).
# Large: pulls in PyTorch + Transformers (~1 GB). Pi 5 with >=4 GB RAM recommended.
#
# transformers is pinned < 5: optimum and TrOCR's tokenizer do not yet work on
# the v5 tokenizer backend.
optimum[onnxruntime]>=1.17,<2
transformers>=4.36,<5
torch>=2.2
sentencepiece>=0.1.99
