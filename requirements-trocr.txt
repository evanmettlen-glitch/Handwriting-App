# Dependencies for the neural backend (--backend trocr).
# ~1 GB. Pi 5 with >=4 GB RAM recommended. No ONNX/optimum needed: the model
# loads directly with torch and the base checkpoint downloads on first run.
torch>=2.2
transformers>=4.36
pillow>=10.0

# Optional speed-up: export to ONNX (scripts/export_trocr_onnx.py) and the app
# will prefer it automatically. Skip unless you need the lower latency —
# optimum pins awkwardly against newer torch releases.
#   pip install "optimum[onnxruntime]"
